export default function MethodBlock() {
  return (
    <section className="panel anchor-panel" id="method">
      <div className="panel-header">
        <h2>Method</h2>
      </div>
      <p>
        Matches are currently ranked from a simple normalized measurement distance.
        This is a scaffold, not a finished body model. Percentiles are placeholder
        outputs until the reference dataset is wired in.
      </p>
    </section>
  );
}
